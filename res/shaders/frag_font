#version 330 core

in vec2 pCoord;
out vec4 FragColor;

uniform sampler2D uTexture;
uniform vec3 uColor;

void main()
{
    vec4 color = texture(uTexture, pCoord);  // 获取纹理颜色
    if (color.r < 0.1) {
        discard;  
    }
    else {
        FragColor = vec4(uColor, color.a); 
    }
    
}
